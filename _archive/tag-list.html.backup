{% comment %}
  Tag List Include
  
  Usage:
  {% include tag-list.html tags=page.sectors type="sector" %}
  
  Parameters:
  - tags: Array of tag names
  - type: Tag type (sector, market, attribute, signal)
{% endcomment %}

{% if include.tags and include.tags.size > 0 %}
<div class="{{ include.type }}-tags tag-list">
  {% for tag in include.tags %}
    {% assign tag_slug = tag | slugify %}
    <a href="{{ site.baseurl }}/{{ page.lang }}/tags/{{ include.type }}s/{{ tag_slug }}/" class="tag {{ include.type }}-tag">
      {{ tag }}
    </a>
  {% endfor %}
</div>
{% endif %}